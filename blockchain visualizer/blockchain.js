class Blockchain {
    constructor() {
        this.chain = [];
        this.addBlock({ data: "Genesis Block" });
    }

    addBlock(data) {
        // code to add a new block to the chain
    }

    isValid() {
        // code to verify the integrity of the chain
    }
}
